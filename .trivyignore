# Trivy IaC ignore list for 10-ci-check.
# Example:
# AVD-AWS-0017  # EKS public endpoint — intentional for demo cluster; tracked in #123

# public endpoint accepted until bastion lands.
AVD-AWS-0040
AVD-AWS-0041
# Managed EKS node groups need broad egress for ECR/STS/registry pulls.
AVD-AWS-0104
AVD-AWS-0039